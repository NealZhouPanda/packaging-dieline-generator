# Contributing

感谢参与维护包装刀模生成器。

## 提交修改前

1. 先说明修改针对哪个箱型，以及是几何规则、界面、导出还是文档。
2. 几何修改必须补对应的单元测试；不要只凭截图调整坐标。
3. 不要提交客户文件、生产 PDF、私有路径、接口响应、Cookie、令牌或其他凭据。
4. 不要把一种箱型的补偿公式复制到另一种箱型。
5. 型号命名优先使用已确认的行业标准编号；没有确认标准来源时，保留项目型号，不要自行编造或冒称行业编号。

## 本地验证

```bash
npm ci
npm test
npm run build:offline
```

如果改了 PDF 导出，还要用真实 PDF 阅读器或 PyMuPDF 检查：页面尺寸、文字是否可提取、刀模是否完整、蓝色刀线和红色虚线压痕是否正确。

## Pull Request

PR 描述请包含：

- 修改目的和适用箱型
- 输入示例或回归样本
- 测试命令及结果
- 如果涉及生产尺寸，说明是否有打样或独立样本验证

没有经过验证的规则可以提交讨论，但不要在界面或 README 中写成“已验证”或“适用于生产”。
