# Contributing

感谢参与维护包装刀模生成器。

## 调整项目内容前的线上同步规则

每次开始调整项目内容前，必须先核对线上最新版本，再进行本地修改：

1. 先检查工作区状态并备份当前本地最新文件，避免覆盖其他成员尚未提交的修改。
2. 拉取并检查远端所有相关分支和提交，不能只查看 `origin/main`。
3. 直接核对 README 中登记的实际 GitHub Pages 页面，以及线上实际使用的 `app/`、`release/`、`docs/` 发布文件；不能把远端 `main` 源码默认当作线上页面的唯一来源。
4. 对比线上页面、远端源码和本地文件的箱型、功能、几何规则、导出逻辑与发布产物。发现远端或线上有更新时，先把本地工作区更新到最新线上基线，再开始本次项目调整。
5. 如果远端源码与实际线上页面不一致，先记录差异并以实际线上版本作为功能核对基准，不能自行选择较旧或箱型更少的源码继续修改。
6. 完成调整后，重新运行测试和离线构建，确认 `app/`、`release/`、`docs/` 产物一致；发布到线上前再向项目负责人确认。

## 提交修改前

1. 先说明修改针对哪个箱型，以及是几何规则、界面、导出还是文档。
2. 几何修改必须补对应的单元测试；不要只凭截图调整坐标。
3. 不要提交客户文件、生产 PDF、私有路径、接口响应、Cookie、令牌或其他凭据。
4. 不要把一种箱型的补偿公式复制到另一种箱型。
5. 型号命名优先使用已确认的行业标准编号；没有确认标准来源时，保留项目型号，不要自行编造或冒称行业编号。

## 本地验证

```bash
npm ci
npm test
npm run build:offline
```

如果改了 PDF 导出，还要用真实 PDF 阅读器或 PyMuPDF 检查：页面尺寸、文字是否可提取、刀模是否完整、蓝色刀线和红色虚线压痕是否正确。

## Pull Request

PR 描述请包含：

- 修改目的和适用箱型
- 输入示例或回归样本
- 测试命令及结果
- 如果涉及生产尺寸，说明是否有打样或独立样本验证

没有经过验证的规则可以提交讨论，但不要在界面或 README 中写成“已验证”或“适用于生产”。
