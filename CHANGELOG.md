# Changelog

规则：每次更新追加一条，写清楚用户反馈、修改内容和验证结果。公开日志只描述本项目自己的实现，不记录外部服务、私有文件或内部排查细节。

## 2026-08-17

- **装柜估算改显实际数**：05 区块主数字改为实际估算 = 理论满载 ×0.88（规则纸箱装载率系数）取整，小字附理论上限；工具用途是给下属做装柜/报价参考，实际效果优先。
- **新增装柜估算**：05 区块可选 20GP/40GP/40HQ/45HQ（内尺寸按公开规格核实，容积交叉验算一致），按当前箱型外尺寸对大/中/小面三种开口分别计算理论满载箱数。算法采用行业装箱计算器的标准做法：外尺寸 6 个朝向 floor 排列取最大值；页面标注实际装柜约为理论 ×0.88（规则纸箱装载率系数，货代经验系数表与装箱计算器口径一致）。外尺寸沿用抛重的刀模→外尺寸换算，因此三种开口的装箱数会有轻微差别。
- **修复白卡纸开口卡片误报超范围**：开口方向计算未把纸型传给生成器，白卡纸 0.4–0.8mm 被按瓦楞纸厚范围（1.5–5mm）拦截，三个开口面全部误报；现已补齐纸型参数。同时每张数量的纸张幅面按纸型区分：瓦楞纸板 1200×2400mm，白卡纸改用大度平板纸 889×1194mm 估算，材料幅面警示同步按纸型判定。
- **移除箱型区块灰色说明**：「选择箱型」标题下的开放箱型列表说明不再显示；同步清理双语表中已过期的对应条目。
- **尺寸与材料分行排版**：长/宽/高并排为第一行（3 列），纸型与楞型/白卡纸厚度为第二行（2 列），自定义纸厚、C001GX 插舌样式等特殊选项独立第三行；各行间距统一为 10px，修复特殊选项行与上方行距不一致的问题。
- **下载区固定与署名移除**：移除下载区的个人署名；下载按钮改为固定在左侧栏底部（侧栏改 flex 纵列、下载区 margin-top:auto），不再随侧栏内容长度上下移动。
- **开口方向面积/每张数量恢复显示**：修复公开箱型开口卡片全部显示「暂不支持」的问题——净面积计算只覆盖内部 0202A，而公开选择器已无该箱型；现公开箱型按刀模展开包络显示面积估算与每张数量，界面说明同步标注估算口径，真实净面积仍待逐箱型独立校准。
- **允许长宽独立输入**：移除“宽 W 必须比长 L 小 2mm 以上”的方向性限制，0202A、0421、K016A 支持宽度大于长度的特殊开口；同步更新三种生成器、输入界面和回归测试，并将三种箱型的长输入范围统一为 50–2000 mm。
- **验证**：`W>L` 组合三箱型均可生成并导出 SVG；`npm test` 通过 61 个测试，`npm run build` 与 `npm run build:offline` 均通过。

## 2026-08-14

- **补充每张数量说明**：在开口方向卡片下方说明“每张”是单位原纸张可切出的包装盒数量，用于用纸和成本估算。
- **修复英文开口数据翻译并移除下载区署名**：英文模式下将面积卡片中的“个/张”翻译为“/ sheet”，并删除页面上的个人署名。
- **按箱型设置展示默认值**：0201 使用 350×190×230 mm / A 楞 5.0，E005C 使用 350×190×230 mm / B 楞 3.0，C001GX 使用 350×190×230 mm / B 楞 3.0；切换箱型时同步加载对应尺寸、纸厚和文件名。
- **修复新箱型开口数据误报超范围**：开口面积刷新不再把多轮廓刀线的面积计算异常误报为尺寸超范围；新箱型改用生成器提供的展开包络面积估算，并保留材料幅面警示。
- **补充型号命名规则**：项目说明明确模型编号优先采用已确认的行业标准编号；`0201` 按 FEFCO 0201 使用，未确认行业标准的型号不冒称标准编号。
- **移除 0202A 用户入口**：按 0201 替换 0202 的决定，界面、默认文件名和默认生成器改为 0201；当前箱型选择只保留 0201、E005C、C001GX。
- **增加中英双语切换**：在现有单文件应用上叠加轻量语言层，覆盖主要界面标签、箱型选项、下载按钮和动态范围/抛重提示；保留中文默认，不改几何计算。
- **准备公开协作**：删除外部服务响应快照、私有生产文件路径和内部逆向记录；将回归数据整理为本地校准快照，README 改为独立实现叙事，补充贡献指南和字体许可证说明；项目代码采用 MIT License。
- **合并多箱型发布版**：发布文件加入 0201、0202A、E005C、C001GX 四种箱型，默认 0201；四种箱型均已实际生成 SVG/PDF。新箱型的开口方向用纸数据在完成独立校准前显示「超范围」。
- **PDF 侧栏移除侧面高区块**：保留几何规则和测试，图纸侧栏不再回显高低位信息。
- **PDF 侧栏行距对齐字号缩放**：标签与深色数值共用按正文尺寸计算的基线步距，避免画布放大后两行贴合。
- **中面最左两条横向压痕去重**：重复绘制会让虚线叠成实线；SVG 和 PDF 导出时只绘制一次，几何校准数据保持不变。
- **下载区署名调整**：曾在下载区显示个人署名，后续已移除。
- **0202A 尺寸语义修正**：PDF 显示刀模尺寸；刀模尺寸=内尺寸+半个纸厚，外尺寸=内尺寸+整纸厚。开箱面外尺寸按两次折叠规则计算。
- **PDF 图纸排版修正**：侧栏字号随展开宽度缩放，标题栏使用真实字宽、灰底分隔线和黑色正文；四周留白，避免刀模贴边裁切。
- **下载文件名修正**：输入框不显示扩展名，下载时按格式补充 `.svg` 或 `.pdf`，避免出现重复扩展名。

## 2026-08-13

- **基础离线工具完成**：单文件 HTML 可在 `file://` 下运行，计算在浏览器本地完成，支持 1:1 mm SVG 和图纸式 PDF。
- **生产样式几何修正**：粘口使用梯形结构，槽底使用生产样式的 R2.5 四分之一弧；预览与下载线型分离。
- **安全门和验证完善**：对尺寸、纸厚和画布容纳范围进行校验；超出本地校准范围时拒绝生成，避免输出未经验证的刀模。
- **界面收敛**：固定 01 选择箱型 → 02 输入内尺寸 → 03 开口方向 → 04 抛重估算的顺序；下载按钮固定在侧栏底部。
- **区分材料类型**：新增瓦楞纸板 / 白卡纸选择；0201 默认 A 楞 5.0，E005C 默认 B 楞 3.0，C001GX 默认白卡纸 0.5 mm，切换箱型时同步加载材料和厚度。
